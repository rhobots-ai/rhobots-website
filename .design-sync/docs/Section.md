---
category: primitives
---

The vertical rhythm primitive: a padded band with a width-constrained container. `tone` picks the surface, `container` the measure, `divided` draws a hairline along the top edge.

```jsx
<Section tone="muted" container="narrow" divided>
  <Heading level={2}>Six products, one platform</Heading>
</Section>
```
