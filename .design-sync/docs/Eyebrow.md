---
category: primitives
---

A short mono label above a heading — the one place uppercase survives the redesign. `tone="accent"` is the olive; `muted` is grey. `number` renders an ordered prefix like `01 /`.

```jsx
<Eyebrow tone="accent">Platform</Eyebrow>
<Eyebrow number="02">Extract</Eyebrow>
```
