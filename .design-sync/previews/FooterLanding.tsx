import { FooterLanding } from 'rhobots-website';

/** The site footer: brand blurb plus Products / Resources / Company link columns and the legal bar. */
export const Default = () => <FooterLanding />;
